# PR Reviews

When doing a PR review and you find a change that needs to be made. Invoke Kelos Operator with labeling the PR with "/iterate" label but if nothing is wrong add "/ready-check" label instead. We are using https://github.com/kelos-dev/kelos to automate this repo further.
